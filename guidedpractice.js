//define variables for budget price 1 and price 2
const budget = (50);
const price1 = (20);
const price2 = (25);
//define a function that will return the sum of the two prices
function sumOfGifts(gift1, gift2) {
   return giftTotal = (gift1 + gift2);
}
//Invoke the sumOfGifts function with the price1 and price2 variables.
// Store the return of this function in a new variable labeled giftTotal.
sumOfGifts(price1, price2)
const giftTotal=sumOfGifts(price1,price2);
function addTax(giftTotal) {
    return giftTotal + (giftTotal * 0.08)
}

console.log(giftTotal)
